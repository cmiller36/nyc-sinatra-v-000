<!DOCTYPE html>
<html lang="en"> 
<head>
  <meta charset="UTF-8">
  <title>Edit a figure</title>
</head>
<body>
  <h1>Edit <%= @figure.name %> </h1>

  <form action="/figures/<%= @figure.id %>" method="POST">
    <input id="hidden" type="hidden" name="_method" value="patch">

      <h1>Figure Name:</h1>
      <input type="text" name="figure[name]" id="figure_name" value="<%= @figure.name %>">
      <br><br>

      <h1>Titles:</h1>
        <ul style="list-style: none;">
        <% Title.all.each do |title| %>
        <li><input type="checkbox" name="figure[title_ids][]" id="title_<%= title.id %>" value="<%= title.id %>" <%='checked' if @figure.titles.include?(title) %>> <%= title.name %></input></li>
        <% end %>
        </ul>

      <label>Add New Title:</label>
      <input type="text" id="new_title" name="title[name]">
      <br><br>

      <h1>Landmarks:</h1>
      <ul style="list-style: none;">
      <% Landmark.all.each do |landmark| %>
        <li><input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" id="landmark_<%= landmark.id %>" <%='checked' if @figure.landmarks.include?(landmark) %>> <%=landmark.name%></input></li>
      <% end %>
      </ul>

      <label>Add New Landmark:</label>
      <input type="text" id="new_landmark" name="landmark[name]">
      <br><br>
      
      <input type="submit" value="Edit Figure">


  </form>

</body>
</html>